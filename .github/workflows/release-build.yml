name: Release

on:
  push:
    tags: ['v*']
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to build (e.g. v0.1.1)'
        required: true
  workflow_call:
    inputs:
      tag:
        description: 'Tag to build (e.g. v0.1.1)'
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: release-${{ inputs.tag || github.ref_name }}
  cancel-in-progress: false

# Required secrets (workflow no-ops them when absent so re-running an
# old release without secrets still works):
#   SIGNING_KEYSTORE              base64 of the upload keystore (.jks)
#   OPENSCAN_KEYSTORE_PASSWORD    keystore password
#   OPENSCAN_KEY_ALIAS            key alias inside the keystore
#   OPENSCAN_KEY_PASSWORD         key password
#   PLAY_SERVICE_ACCOUNT_JSON     service account JSON contents; presence
#                                 also gates the Play upload step
jobs:
  release:
    name: Build & publish release
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: write
      packages: write
    env:
      TAG_NAME: ${{ github.ref_type == 'tag' && github.ref_name || inputs.tag }}
      HAS_SIGNING_KEYSTORE: ${{ secrets.SIGNING_KEYSTORE != '' }}
      HAS_PLAY_CREDENTIALS: ${{ secrets.PLAY_SERVICE_ACCOUNT_JSON != '' }}
    steps:
      - uses: actions/checkout@v7.0.0
        with:
          ref: ${{ github.ref_type == 'tag' && github.ref_name || inputs.tag }}

      - uses: ./.github/actions/setup
        with:
          cache-read-only: 'true'

      - name: Decode signing keystore
        if: env.HAS_SIGNING_KEYSTORE == 'true'
        env:
          SIGNING_KEYSTORE: ${{ secrets.SIGNING_KEYSTORE }}
        run: |
          set -euo pipefail
          keystore="${RUNNER_TEMP}/upload.jks"
          printf '%s' "$SIGNING_KEYSTORE" | base64 -d > "$keystore"
          echo "OPENSCAN_KEYSTORE_PATH=$keystore" >> "$GITHUB_ENV"

      - name: Assemble release
        env:
          OPENSCAN_KEYSTORE_PASSWORD: ${{ secrets.OPENSCAN_KEYSTORE_PASSWORD }}
          OPENSCAN_KEY_ALIAS: ${{ secrets.OPENSCAN_KEY_ALIAS }}
          OPENSCAN_KEY_PASSWORD: ${{ secrets.OPENSCAN_KEY_PASSWORD }}
        run: ./gradlew :app:assembleRelease :app:bundleRelease --stacktrace

      - name: Publish to Play (internal track)
        if: env.HAS_PLAY_CREDENTIALS == 'true' && env.HAS_SIGNING_KEYSTORE == 'true'
        env:
          ANDROID_PUBLISHER_CREDENTIALS: ${{ secrets.PLAY_SERVICE_ACCOUNT_JSON }}
          OPENSCAN_KEYSTORE_PASSWORD: ${{ secrets.OPENSCAN_KEYSTORE_PASSWORD }}
          OPENSCAN_KEY_ALIAS: ${{ secrets.OPENSCAN_KEY_ALIAS }}
          OPENSCAN_KEY_PASSWORD: ${{ secrets.OPENSCAN_KEY_PASSWORD }}
        run: ./gradlew :app:publishBundle --stacktrace

      - name: Stage release assets
        id: stage
        run: |
          set -euo pipefail
          mkdir -p _release
          version="${TAG_NAME#v}"
          shopt -s nullglob
          for f in app/build/outputs/apk/release/*.apk; do
            name=$(basename "$f" .apk)
            suffix=""
            case "$name" in *-unsigned) suffix="-unsigned" ;; esac
            cp "$f" "_release/openscan-${version}${suffix}.apk"
          done
          for f in app/build/outputs/bundle/release/*.aab; do
            cp "$f" "_release/openscan-${version}.aab"
          done
          ls -la _release/
          echo "version=${version}" >> "$GITHUB_OUTPUT"

      - name: Attach assets to GitHub release
        if: (github.event_name == 'push' || github.event_name == 'workflow_call') && env.HAS_SIGNING_KEYSTORE == 'true'
        uses: softprops/action-gh-release@v3.0.1
        with:
          tag_name: ${{ env.TAG_NAME }}
          files: |
            _release/*.apk
            _release/*.aab
          fail_on_unmatched_files: true

      - name: Upload workflow artifact
        if: github.event_name == 'workflow_dispatch' || env.HAS_SIGNING_KEYSTORE != 'true'
        uses: actions/upload-artifact@v7.0.1
        with:
          name: release-${{ steps.stage.outputs.version }}
          path: _release/
          if-no-files-found: ignore
